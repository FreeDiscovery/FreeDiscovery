Command line interface
======================

Installing the FreeDiscovery Python packages also makes available a `freediscovery` CLI command which can be used to start the server and to manage trained models.

.. autoprogram:: freediscovery.__main__:main(return_parser=True)
   :prog: freediscovery
